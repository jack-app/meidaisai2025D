import { describe, it, expect } from 'vitest';
import GameDataProvider from './gameData';

describe('gameData', () => {
    it('GameDataProvider should be defined', () => {
        expect(GameDataProvider).toBeDefined();
    });
})